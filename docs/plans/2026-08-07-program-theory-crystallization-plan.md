# Program Theory Crystallization Plan

Status: completed — theory ratified Active and wired into the startup
order; see Execution Log
Class: 5 — creates `docs/program-theory.md` (new normative conceptual
surface) and amends [DOM-2]/[DOM-3] plus the canonical startup order
Hardening: N/A — no [DOM-5] risky trigger fires (docs-only; no
behavior, contract, storage, or CLI change)

## Goal

Crystallize Taut's product program theory from the README (the
declared product contract, written before any spec or code), the
[TAUT-*]/[IAN-*]/[SUM-*]/[SRCH-*] specs, plans, lessons, and owner
dialogue; ratify it Active; wire it into the canonical startup order,
`context.index.yaml`, and [DOM-2]/[DOM-3].

## Source Documents

- `README.md` (founding contract), `docs/specs/02-taut-core.md`,
  `docs/specs/03-identity-addressing-notifications.md`,
  `docs/specs/04-summon.md`, `docs/specs/06-search.md`,
  `docs/lessons.md`, plan review records under `docs/plans/`
- Method: the agent-theory hub's crystallize-program-theory interview
  spine (hub cited by name), executed as owner dialogue 2026-08-07

## Execution Log

- 2026-08-07: Draft crystallized with four OPEN items; owner resolved
  all four (use-surface simplicity adopted; durable-alternative
  analysis directed; theory-first founding recorded — README written
  before any spec or code; SimpleBroker's product-section registry
  mechanism adopted, with registry creation and README contract
  re-homing named as follow-up work).
- 2026-08-07: Owner-directed analysis ranked eleven candidate durable
  alternatives; owner adopted five (A1 notification pointers, A2
  search parity, A3 no-auth, A4 no-daemon-required as refined with
  the two-condition reconsideration, A5 cursor-last write ordering);
  five architecture/contract-scope candidates routed to owning specs
  and implementation docs as future work; two parked as process
  scope.
- 2026-08-07: Independent semantic review (codex, different family,
  read-only): ADOPT-WITH-EDITS, ten exact edits — all applied
  (header boundary/verification, dual-owner concept table with
  verified spec mappings, live-watcher row, invariant rephrasing for
  the no-daemon non-goal and A4, A5 retitle, notification kinds
  corrected to include replies, reactions row and two principles
  simplified, unread falsifier made observable, quote attribution
  corrected).
- 2026-08-07: Owner ratified; Status flipped to Active; wired into
  `docs/agent-context/README.md` canonical startup order (step 2),
  `context.index.yaml` `read_order` (first) with role
  `product_program_theory`, and [DOM-2]/[DOM-3]. Gates:
  check-plan-status-index 0, check-dom15-fixtures 0, coalesce-check
  0, check-doc-paths (uv) 0.

## Out of Scope

- The product-section registry and README contract re-homing (named
  follow-up; part of the information-architecture unit).
- The agent-theory propagation wave (its own plan).
- Any product behavior change.
